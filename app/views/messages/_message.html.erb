<div class="msg-cont">
  <p>
    <strong>
      <%= link_to message.user.first_name, user_path %>
    </strong>
    <% if current_user %>
    <%= link_to 'Delete', room_message_path(message.room, message), method: :delete, data: { confirm: 'Are you sure?' }, class: "del-msg" %>
    <% end %>
    <small>
      <br>
      <em>
        <%= "#{time_ago_in_words(message.created_at)} ago" %>
      </em>
    </small>
  </p>
  <p>
    <%= message.content %>
  </p>
</div>
